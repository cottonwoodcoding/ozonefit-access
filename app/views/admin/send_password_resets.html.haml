.center
  %h3 Send Reset Emails
  - User.by_name.each do |user|
    = form_tag(generate_new_password_email_path(user)) do
      = user.name
      %br
      = submit_tag 'Send Reset Password Email', class: 'btn', confirm: 'Are you sure you want to email reset password instructions?'